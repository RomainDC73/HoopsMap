// Add an event listener that runs when the DOM content is fully loaded
document.addEventListener('DOMContentLoaded', function () {
    // Replace all Feather icons with their corresponding SVG elements
    feather.replace();
});
